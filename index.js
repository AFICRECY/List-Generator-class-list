const CLI = require('./lib/cli.js');

const cli = new CLI();

cli.run();

class Component {
    constructor(render, renderInnerHTML) {
        this.render = (err, "Child class must implement" + render())
    }
renderinnerHTML() => {
for (var i = 0; i < toChildren.length; i++) {

}
}

}